@model IEnumerable<Product>

<div class="product-items">
    <div class="row product-active">
        @for (int i = 0; i < (int)Math.Ceiling((decimal)Model.Count() / 2); i++)

        {
            @foreach (Product product in Model.Skip(i * 2).Take(2))
            {
                <div class="col-lg-2">
                    <div class="single-product mt-30">
                        <div class="product-image">
                            <a href="single-product.html">
                                <img class="first-image" src="~/assets/images/product/@product.MainImage" alt="product">
                                <img class="second-image" src="~/assets/images/product/@product.HoverImage" alt="product">
                            </a>
                            <ul class="product-meta">
                                <li><a class="addtobasket" asp-controller="basket" asp-action="addtobasket" asp-route-id="@product.Id"><i class="icon ion-bag"></i></a></li>
                                <li><a href="#"><i class="icon ion-android-favorite-outline"></i></a></li>
                                <li><a href="#"><i class="icon ion-android-options"></i></a></li>
                                <li><a class="productModalBtn" data-toggle="modal" data-target="#productQuickModal" asp-controller="shop" asp-action="modal" asp-route-id="@product.Id"><i class="icon ion-android-open"></i></a></li>
                            </ul>
                            @if (product.DiscountedPrice > 0)
                            {
                                <span class="discount-product">-@((((product.Price * 100) /product.DiscountedPrice)-100).ToString("0.00"))%</span>
                            }
                        </div>
                        <div class="product-content">
                            <ul class="product-rating">
                                <li class="rating-on"><i class="fas fa-star"></i></li>
                                <li class="rating-on"><i class="fas fa-star"></i></li>
                                <li class="rating-on"><i class="fas fa-star"></i></li>
                                <li class="rating-on"><i class="fas fa-star"></i></li>
                                <li class="rating-on"><i class="fas fa-star"></i></li>
                            </ul>
                            <h4 class="product-title"><a href="single-product.html">Originals Kaval Wind breaker Winter Jacket eu...</a></h4>
                            <div class="product-price">
                                @if (product.DiscountedPrice > 0)
                                {
                                    <span class="regular-price ">€@product.Price</span>
                                    <span class="price-sale">€@product.DiscountedPrice</span>
                                }
                                else
                                {
                                    <span class="regular-price ">€@product.Price</span>
                                }
                            </div>
                        </div>
                    </div> <!-- single product -->

                </div>

            }


        }

    </div> <!-- row -->
</div> <!-- product items -->